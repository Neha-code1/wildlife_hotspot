import streamlit as st

st.set_page_config(page_title="Live Animal Detection")

st.title("🦌 Live Animal Detection")

st.warning("This feature is temporarily unavailable in the cloud deployment due to missing system libraries required by OpenCV/YOLO.")